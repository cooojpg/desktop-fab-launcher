# Mouse Command Launcher - DESIGN

## 目的

マウスの右ボタン長押しをトリガーとして入力受付モードを起動し、
続く右／左クリックの組み合わせによってユーザー定義コマンドを実行する。

キーボードに手を伸ばさず、手を動かさずに高速実行できることを目的とする。
マウスジェスチャー（移動操作）を採用しないことで、物理移動を排除し、
より素早く・安定して操作することを可能にする。

---

## 基本コンセプト

- マウスのみで完結
- 物理移動ゼロで実行可能
- 誤爆しにくい設計
- 視覚フィードバックは最小限
- 筋肉記憶で操作可能

---

## 入力トリガー仕様

### 起動条件

- マウスを動かさずに右ボタンを一定時間押し続ける
- 押下時間の閾値はユーザー設定可能
- 初期値は検証によって決定する

### 右クリック無効化

- 長押しが成立した場合、通常の右クリックは無効化する
- 閾値未満で離した場合は通常の右クリックとして扱う

### マウス移動の扱い

- トリガー判定中は「移動しない」ことが条件
- 入力受付中はマウス移動はキャンセル条件にしない

---

## 入力受付モード（Armed Mode）

### 受付内容

- 左クリック
- 右クリック

### 最大入力回数

- 2回～5回

### キャンセル条件

以下のいずれかでキャンセルされる：

- 5回クリック到達（入力終了扱い）
- 一定時間無入力（設定可能）
- 途中で別ボタンを押す（例：中クリック）
- システムフォーカス喪失

※ マウス移動はキャンセル条件に含めない

---

## 実行タイミング

クリック列は可変長入力であるため、
一致した時点では実行しない（prefix問題を回避するため）。

### 実行判定

入力終了時に：

- 完全一致するコマンドが存在する → 実行
- 存在しない → キャンセル

---

## 表示仕様（オーバーレイ）

### 表示位置

- カーソルの近くに表示
- 画面端にある場合は画面内に収まるよう自動補正

### 表示内容

- 現在入力されたクリック列を2色の円を横並び表示して表現


### デザイン方針

- 極力シンプル
- 半透明
- 入力アニメーション
- 成功／キャンセル時エフェクト

---

## コマンド仕様

- 仮想デスクトップ切り替え
- アプリ起動
- URLを開く
- キー入力（ショートカット送信）
- 定型テキスト入力
- 戻る / 進む
- ウィンドウ最大化／最小化
- ウィンドウ配置
- モニタ間移動
- 音量調整
- メディア操作
- クリップボード履歴呼び出し
- HTTPリクエスト送信
- 外部API呼び出し
- ローカルAIエージェント呼び出し
- PowerShellスクリプト実行（許可制）
- 画面ロック

---

## 設定項目

- コマンドマッピング
- 長押し時間
- 無入力タイムアウト時間
- 表示形式
- 表示サイズ
- 表示透明度


---

# 将来的に追加可能な機能

## 入力拡張

- 中クリック対応
- ダブルクリックを1入力として扱うモード
- ホイール回転を入力として扱う
- 修飾キー併用モード（Ctrl/Shift）
- 押下と離上を区別する高度入力モード
- 長押し中のみ入力受付モード
- 離した瞬間確定モード
- 連続モード（終了せず次入力へ）

---

## コンテキスト依存

- アプリ別プロファイル
- フルスクリーン時無効
- ゲーム中無効
- 特定アプリ上のみ有効
- ウィンドウクラス名による条件分岐


---

## 安全性配慮

- スクリプト実行はデフォルト無効
- 危険操作は確認表示
- 実行ログ保存
- 管理者権限要求の明示
- 異常終了時の安全復帰

---
